[package]
license = "AGPL-3.0-only"
name = "factory-core"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared" }
async-trait = "0.1"
anyhow = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
bastion = { path = "../bastion", features = ["fs"] }
chrono = { workspace = true }
